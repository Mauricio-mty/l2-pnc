spring:
  application:
    name: exercise

  datasource:
    url: jdbc:postgresql://localhost:5432/STUDENT_BD
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect